// const { handler } = require('.');
// const { createMockEvent } = require('./test.helpers.js');

/*
Note that we are going to mock the SESV2 module directly in this example, as opposed to using Nock to mock the network request it makes. This may be a little less flexible than before, but the SDK is the only viable way to make these requests to AWS so it's a sensible option in this case.
*/

test('Delete this test and bring in your business requirements here', () => {});
